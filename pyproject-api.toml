[project]
name = "autoresearch-inference-api"
version = "1.0.0"
description = "AutoResearch SDPA CPU-only inference API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0.0",
    "fastapi>=0.100.0",
    "uvicorn[standard]>=0.23.0",
    "pydantic>=2.0.0",
    "starlette>=0.27.0",
]